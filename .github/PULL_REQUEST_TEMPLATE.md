## What this changes

## Why

## Checklist
- [ ] `ruff check .` passes locally
- [ ] `pytest -v` passes locally
- [ ] If I changed the scoring formula, I updated its docstring and `tests/test_scoring.py`
- [ ] If I added config, it lives in `config.toml` with a sensible default, not a new required env var
